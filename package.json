{
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*"
  ],
  "name": "limiting-corn",
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  }
}
